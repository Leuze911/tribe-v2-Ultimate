# TRIBE v2 - E2E Test: Create POI Flow
# NOTE: Maestro has touch handling issues with MapLibre overlay.
# Button works correctly with manual/adb taps.
# This test verifies UI elements are present.
appId: com.tribe.app
---

# Test: Verify POI creation UI elements
- launchApp
- waitForAnimationToEnd
- waitForAnimationToEnd

# Map is visible with all UI elements
- assertVisible: "Rechercher un lieu..."
- assertVisible: "Tous"
- assertVisible: "Restaurants"
- assertVisible: "Cafes"
- assertVisible: "Niveau 1"

# Verify add POI button exists (accessibility label)
- assertVisible: "Ajouter un POI"

# Verify locate button exists
- assertVisible: "Ma position"
